services:
  - type: web
    name: ays-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: "gunicorn api:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        sync: false
